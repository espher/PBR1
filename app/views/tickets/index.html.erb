<%= render "layouts/authenticated" %>
<div class="pageheader">
            
            <div class="pageicon"><span class="iconfa-exclamation-sign"></span></div>
            <div class="pagetitle">
                <%= link_to 'Crear Ticket', url_for(:action => 'new', :controller => 'tickets') , :class => "btn btn-primary btn-large pull-right" %>
<h2>Tickets</H2>
            </div>
        </div><!--pageheader-->


    
    <div class="maincontent">
            <div class="maincontentinner">
                <div class="row-fluid">
    <table class="table table-striped table-bordered table-condensed table-hover">
<thead>
  <tr>
    <th colspan="1">Titulo</th>
    <th colspan="1">Creado Por</th>
    <th colspan="1">Fecha de Creacion</th>
    <th colspan="1">Fecha de Modificacion</th>
    <th colspan="1">Acciones</th>
  </tr>
</thead>
 <% unless @ticket.empty? %>
    <% @ticket.each do |ticket| %>
      <tr>
        
        
        <td><%= link_to ticket.title, ticket %></td>
        <td><%= ticket.description %></td>
        <td><%= ticket.description %></td>
        <td><%= ticket.description %></td>
           
<td>

      <%= link_to(ticket, class: "btn btn-mini btn-danger pull-right", :method => :delete) do %>
            Borrar <i class="icon-trash icon-white"></i>
          <% end %>

          <%= link_to(edit_ticket_path(ticket), class: "btn btn-mini btn-primary pull-right") do %>
            Edit <i class="icon-edit icon-white"></i>     
          
          <% end %>

                 </td>
      </tr>

       
      
    <% end %>
  <% else %>
    <tr>
      <td colspan="0">No a creado ticket</td>
    </tr>
  <% end %>
  </tbody>
</table>


