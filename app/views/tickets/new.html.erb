<%= render "layouts/authenticated" %>
<%= form_for [@ticket] do |f| %>

<p>
<%= f.label :title %><br>
<%= f.text_field :title %>
</p>
<p>
<%= f.label :description %><br>
<%= f.text_area :description %>
</p>

 <%= f.submit "Crear", :class =>"btn btn-primary pull-right" %>
<% end %>